<lu class="user comment">
<% post.comments.each do |comment| %>
    <li class="content-comment">
    <p class="content-comment"><%= User.find(comment.author_id).name %>:<%= comment.text %></p>
    </li>
    <% end %>
</lu>